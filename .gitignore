bin/__pycache__/
.claude/
.venv/
__pycache__/

# Real deployments carry infra identifiers (VM ids, addresses, oracle markers).
# The live copy lives at ~/.config/harbor/config.toml; `harbor init` recreates it.
deployments/*/config.toml
.private-markers
